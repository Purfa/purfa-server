'use strict';

const Promise = require('bluebird');
const configuration = require('./image-storage-config');

/**
	This has to be called before using cloudinary image hosting
	service.
*/
module.exports.configure = () => {

};

/**
	Uploads an image and returns a promise with the operation result 
*/
module.exports.upload = (image) => {
	
};	